#Generating a new SSH key


cd ~/.ssh

ssh-keygen -t ed25519 -C "your_email@example.com"

#Adding your SSH key to the ssh-agent
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/"private.key"

#About SSH keys to github account setting
ssh -T git@github.com
